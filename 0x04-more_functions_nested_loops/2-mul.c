#include "main.h"
#include <stdio.h>

/**
 * mul - mutliplies two integer
 * @a: - the first integer
 * @b: - the second integer
 * Return: always 0
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
